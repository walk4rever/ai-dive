# Compatibility Note

Canonical signals reference has moved to:

1. `skills/ai-dive-api/references/signals.md`
2. `docs/api-guide.md` (`POST /api/signals`, `DELETE /api/signals`)

Use `signal_date` (not `date`) for signal business date.
